---
description: Coordinated version bump for Yak theme (roadmap)
---

# update-ui (stub)

Bump the public theme version in all required locations.

## Intended steps (to implement)

1. Set new version in `style.css` (`Version:` header).
2. Add changelog section to `readme.md` and `README.txt`.
3. Optionally align any screenshot or GitHub release notes (outside this repo automation).

Manual checklist today:

- [ ] `style.css` — `Version: x.y.z`
- [ ] `readme.md` — header **Version** + **Changelog** section
- [ ] `README.txt` — `Version:` line + `= Version x.y.z =` changelog block
