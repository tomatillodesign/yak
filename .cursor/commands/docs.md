---
description: Keep Yak theme documentation aligned with code (roadmap)
---

# docs (stub)

Synchronize documentation with the theme as features change.

## Intended steps (to implement)

1. Ensure `readme.md` changelog reflects recent `style.css` version and notable behavior changes.
2. Mirror key bullets into `README.txt` for WordPress.org-style packages if needed.
3. Update `docs/tooling.md` when new commands or conventions are added.

For now, edit `readme.md` / `README.txt` by hand when bumping version.
